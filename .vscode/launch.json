{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python Debugger: FastAPI",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "cwd": "${workspaceFolder}/backend",
      "args": ["main:app", "--reload", "--host", "0.0.0.0", "--port", "8008"],
      "jinja": true,
      "python": "${workspaceFolder}/backend/venv/bin/python"
    }
  ]
}
